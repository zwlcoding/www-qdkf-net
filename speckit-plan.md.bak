# 六爻预测项目添加执行计划

## 步骤 1：在「我的博客」卡片后插入新卡片
- 操作：修改 `/root/workspaces/www-qdkf-net/index.html`
- 具体：在「我的博客」</li> 结束后，插入「六爻预测」卡片

## 步骤 2：验证修改
- 检查 HTML 结构完整性
- 确认链接地址正确
- 确认样式类名无误

## 执行命令
```bash
# 步骤1：编辑 index.html，在博客卡片后添加六爻预测卡片
# 使用 edit 工具精确定位插入位置
```

## 回滚方案
如有问题，使用 git checkout index.html 恢复原状
